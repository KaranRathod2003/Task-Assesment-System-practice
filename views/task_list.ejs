<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="px-4 py-2 bg-zinc-900 w-full min-h-screen text-white">
        <h1>Task List</h1>
        <ul>
            <% tasks.forEach(task=> { %>
                <li>
                    <strong>Title:</strong>
                    <%= task.title %><br>
                        <strong>Description:</strong>
                        <%= task.description %><br>
                            <strong>Status:</strong>
                            <%= task.status %><br>
                                <% if (task.assignedTo) { %>
                                    <strong>Assigned To:</strong>
                                    <%= task.assignedTo.username %><br>
                                        <% } %>
                </li>
                <% }); %>
        </ul>

    </div>
</body>

</html>